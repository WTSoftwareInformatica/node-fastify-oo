/*
export interface IUserRoutes {
  registerRoutes(app: any): Promise<void>;
}
*/


// ATENÇÃO: Essa inferface pode ser aprimorada para conter o contrato de cada uma das rotas "obrigatórias" para o módulo? 
// Talvez isso faria sentido em criar nomes de rotas mais específicos para usar no userFastifyRoutes, tipo createUser, editUser, etc.