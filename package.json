{
  "name": "fastify-oo",
  "version": "1.0.0",
  "description": "Teste para uso de OO e desacoplamento do framework web",
  "main": "server.js",
  "scripts": {
    "build": "rimraf node_modules/.cache && rimraf dist && tsc",     
    "clean": "rimraf node_modules/.cache && rimraf dist && rimraf node_modules",     
    "cleanbuild": "npm install && rimraf node_modules/.cache && rimraf dist && rimraf node_modules && npm install && npx prisma generate && tsc",     
    "dev": "tsx watch src/server.ts", 
    "start": "node dist/server.js",     
    "test": "tsx vitest"               
  },
  "author": "Wladimir Santos",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^6.14.0",
    "fastify": "^5.5.0"
  },
  "devDependencies": {
    "@types/node": "^24.2.1",
    "prisma": "^6.14.0",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "tsx": "^4.20.4",
    "typescript": "^5.9.2"
  },
  "keywords": []
}
